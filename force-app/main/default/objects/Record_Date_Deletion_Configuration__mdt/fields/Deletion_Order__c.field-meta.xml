<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Deletion_Order__c</fullName>
    <description>The deletion order is needed to delete the objects in correct order, so that children records are deleted before the parent record.</description>
    <externalId>false</externalId>
    <fieldManageability>DeveloperControlled</fieldManageability>
    <inlineHelpText>The deletion order is needed to delete the objects in correct order, so that children records are deleted before the parent record.</inlineHelpText>
    <label>Deletion Order</label>
    <precision>3</precision>
    <required>true</required>
    <scale>0</scale>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
