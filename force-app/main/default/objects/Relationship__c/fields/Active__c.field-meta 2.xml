<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Active__c</fullName>
    <description>The relationship is active if the start date is in the past and the end date is empty or in the future</description>
    <formula>IF(
AND(Start_Date__c &lt;= TODAY(),
OR(End_Date__c &gt;= TODAY(),
ISBLANK(End_Date__c)
)
), True, False)</formula>
    <inlineHelpText>The relationship is active if the start date is in the past and the end date is empty or in the future</inlineHelpText>
    <label>Active</label>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
