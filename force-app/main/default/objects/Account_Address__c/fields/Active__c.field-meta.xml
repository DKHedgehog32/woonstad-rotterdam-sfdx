<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Active__c</fullName>
    <description>This checkbox is selected if the account address is active based on entered start and end date.</description>
    <formula>IF(
AND(Start_Date__c &lt;= TODAY(),
OR(End_Date__c &gt;= TODAY(),
ISBLANK(End_Date__c)
)
), True, False)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>This checkbox is selected if the account address is active based on entered start and end date.</inlineHelpText>
    <label>Active</label>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
